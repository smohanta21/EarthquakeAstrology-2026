---
phase: 04-web-app-and-deployment
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
must_haves:
  truths:
    - "Vercel build completes without errors"
    - "Deployed URL serves the calendar page with disclaimer visible"
    - "predictions.json served from CDN as static asset (not bundled into serverless function)"
    - "/methodology route works on deployed URL"
  artifacts: []
  key_links:
    - from: "Vercel build"
      to: "web/ directory"
      via: "Root Directory = web in Vercel project settings"
      pattern: "Root Directory"
---

<objective>
Deploy the complete Next.js app to Vercel and verify the production build succeeds with all assets within size limits.

Purpose: Fulfill DEPLOY-01 (Vercel deployment from web/) and DEPLOY-02 (build succeeds, assets within limits).
Output: Live Vercel URL serving the earthquake astrology calendar.
</objective>

<execution_context>
@/Users/adityasrivatsan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adityasrivatsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-app-and-deployment/04-CONTEXT.md
@.planning/phases/04-web-app-and-deployment/04-RESEARCH.md
@.planning/phases/04-web-app-and-deployment/04-02-SUMMARY.md
@.planning/phases/04-web-app-and-deployment/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build and verify output</name>
  <files></files>
  <read_first>
    - web/package.json (verify build script exists)
    - web/next.config.ts (verify no webpack config that would break Turbopack)
    - .planning/phases/04-web-app-and-deployment/04-RESEARCH.md (Vercel deployment pattern, size limits)
  </read_first>
  <action>
1. Run `cd web && npm run build` and capture full output. This is the same build Vercel will run.

2. Verify build output:
   - No TypeScript errors
   - No module resolution errors (especially no "Can't resolve 'fs'" — would indicate a Client Component using fs)
   - Static routes generated for `/` and `/methodology`
   - Build completes successfully

3. Check build output size:
   - `du -sh web/.next/` — should be well under 250 MB (Vercel serverless function limit)
   - `ls -lh web/public/data/predictions.json` — confirm 218 KB (within Vercel 100 MB source limit)
   - `ls -lh web/public/data/eval_report.json` — confirm small

4. Verify predictions.json is NOT in the serverless function bundle:
   - `find web/.next/server -name "predictions.json" 2>/dev/null` — should return nothing (file is in public/, served from CDN)

This task runs the build Claude will verify passes. The next task is the human checkpoint for actual Vercel deployment.
  </action>
  <verify>
    <automated>cd /Users/adityasrivatsan/Documents/EarthquakeAstrology-2026/web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - Build output shows routes for "/" and "/methodology"
    - `find web/.next/server -name "predictions.json"` returns empty (not bundled in serverless)
    - web/public/data/predictions.json size < 1 MB
    - web/.next/ total size < 50 MB
  </acceptance_criteria>
  <done>Production build passes, static routes generated, predictions.json confirmed as CDN asset not serverless bundle</done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Vercel and verify production URL</name>
  <files></files>
  <action>Deploy to Vercel using either the Dashboard (import repo, set Root Directory to `web`) or CLI (`cd web && npx vercel --yes`). Human verifies the deployed URL serves the calendar, methodology page, disclaimer, and predictions.json as a static asset.</action>
  <verify>
    <automated>cd /Users/adityasrivatsan/Documents/EarthquakeAstrology-2026/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Vercel deployment live, all pages rendering, predictions.json served from CDN</done>
  <what-built>Complete Next.js 16 app with calendar, methodology page, and disclaimer. Production build verified locally.</what-built>
  <how-to-verify>
    1. Deploy to Vercel -- two options:
       a. Vercel Dashboard: Import GitHub repo, set Root Directory to `web`, deploy
       b. Vercel CLI: Run `cd web && npx vercel --yes` (will prompt for project settings; set Root Directory to `web`)
    2. After deploy completes, visit the Vercel URL
    3. Verify disclaimer banner visible at top of page
    4. Verify calendar renders 10 months (March-December 2026)
    5. Verify March 8 shows as orange (high-risk) cell
    6. Click March 8 -- verify detail panel opens with regions and aspects
    7. Visit /methodology -- verify metrics table and confusion matrix render
    8. Check Vercel deployment log for any warnings about bundle size
    9. Visit /data/predictions.json on the deployed URL -- verify it is served as a static file from CDN
  </how-to-verify>
  <resume-signal>Type "approved" with the Vercel URL, or describe deployment issues</resume-signal>
</task>

</tasks>

<verification>
- Production build exits 0
- Vercel deployment succeeds
- Live URL serves calendar, methodology page, and disclaimer
- predictions.json accessible as static CDN asset at /data/predictions.json
- No serverless function size warnings in Vercel build log
</verification>

<success_criteria>
- Vercel build completes without errors (DEPLOY-02)
- App deployed from web/ directory (DEPLOY-01)
- All static assets within Vercel size limits (DEPLOY-02)
- predictions.json served from public/data/ not bundled into serverless function (DEPLOY-01)
- Live URL confirmed working by human verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-app-and-deployment/04-04-SUMMARY.md`
</output>
