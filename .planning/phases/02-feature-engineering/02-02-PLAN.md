---
phase: 02-feature-engineering
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - pipeline/features/engineering.py
  - tests/test_engineering.py
autonomous: true
requirements: [FEAT-04, FEAT-05]

must_haves:
  truths:
    - "Given a USGS lat/lon, grid cell is floor(lat/5)*5 and floor(lon/5)*5 as integers"
    - "Active cells set contains only cells where at least one M5.5+ event occurred across 1900-2026"
    - "Country extracted from USGS place field is the last comma-delimited token; NaN/None returns 'Unknown'"
    - "EQ index is a pandas Series with MultiIndex (date, grid_lat, grid_lon) where value is always 1"
    - "Multiple USGS events on the same date in the same cell produce a single entry in the EQ index (no duplicates)"
    - "country_map dict maps (grid_lat, grid_lon) tuples to the most-common country label for that cell"
  artifacts:
    - path: "pipeline/features/engineering.py"
      provides: "compute_grid_coords, build_active_cells, extract_country, build_eq_index, build_country_map — all fully implemented"
      exports: ["compute_grid_coords", "build_active_cells", "extract_country", "build_eq_index", "build_country_map"]
  key_links:
    - from: "pipeline/features/engineering.py:build_eq_index"
      to: "data/raw/usgs_earthquakes.csv"
      via: "pd.read_csv + grid coord computation"
      pattern: "grid_lat.*grid_lon.*date.*MultiIndex"
    - from: "pipeline/features/engineering.py:build_active_cells"
      to: "data/raw/usgs_earthquakes.csv"
      via: "full-dataset lookup (no leakage — purely a cell existence check)"
      pattern: "set.*grid_lat.*grid_lon"
---

<objective>
Implement all USGS-side preprocessing functions in engineering.py: grid cell computation, active cell discovery, country extraction, EQ index construction, and country map building.

Purpose: These functions produce the two critical lookup structures (eq_index and active_cells) that the Wave 2 chunked matrix builder broadcasts against. Without them, Plan 04 cannot assign EQIndicator or grid geography.
Output: Four fully-implemented functions in pipeline/features/engineering.py; TestGridCells, TestCountryParsing, TestEQIndicator, TestEQIndicatorCollapse tests all pass.
</objective>

<execution_context>
@/Users/tirunelvelynagarajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tirunelvelynagarajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-feature-engineering/02-01-SUMMARY.md
@.planning/phases/02-feature-engineering/02-RESEARCH.md
@pipeline/features/engineering.py
@tests/test_engineering.py
@pipeline/data/usgs.py

<interfaces>
<!-- From pipeline/data/usgs.py (established Phase 1 patterns) -->
<!-- USGS CSV columns: time, latitude, longitude, depth, mag, place -->
<!-- Date parsing: pd.to_datetime(df['time']).dt.normalize().dt.date -->
<!-- Grid cell: floor(lat/5)*5, floor(lon/5)*5 — verified from RESEARCH.md -->

From data/raw/usgs_earthquakes.csv (39,514 rows):
  Columns: time (ISO8601 with Z), latitude (float), longitude (float), depth (float), mag (float), place (str, nullable)
  Example place values:
    "Southern Sumatra, Indonesia" -> country "Indonesia"
    "Bismarck Sea" -> country "Bismarck Sea"
    None/NaN -> country "Unknown"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement grid cell and active cells functions</name>
  <files>pipeline/features/engineering.py, tests/test_engineering.py</files>
  <behavior>
    - compute_grid_coords(lat=35.7, lon=139.2) -> (35, 135) [floor(35.7/5)*5=35, floor(139.2/5)*5=135]
    - compute_grid_coords(lat=-3.0, lon=100.5) -> (-5, 100) [floor(-3/5)*5=-5, floor(100.5/5)*5=100]
    - compute_grid_coords(lat=0.0, lon=0.0) -> (0, 0)
    - build_active_cells(usgs_df) returns a Python set of (int, int) tuples
    - build_active_cells result contains (35, 135) if any row in usgs_df has lat in [35,40) and lon in [135,140)
    - build_active_cells result does NOT contain cells with no events (not just zeros for those cells — excluded entirely)
    - Actual active cell count from full USGS data is 901 (log this at INFO level)
  </behavior>
  <action>
    Implement `compute_grid_coords(lat: float, lon: float) -> tuple[int, int]` using `int(np.floor(lat / 5) * 5)` and `int(np.floor(lon / 5) * 5)`. Import numpy as np at module top.

    Implement `build_active_cells(usgs_df: pd.DataFrame) -> set[tuple[int, int]]`:
    - Compute grid_lat and grid_lon columns using compute_grid_coords logic (vectorized with np.floor)
    - Return `set(zip(grid_lats.astype(int), grid_lons.astype(int)))`
    - Log: `logger.info(f"Active cells (all-time): {len(cells)}")`

    Update `tests/test_engineering.py` TestGridCells and the build_active_cells test to make them pass (remove xfail markers, add real assertions matching the behavior above).

    Use the module-level logger established in Phase 1 pattern: `logger = logging.getLogger("pipeline.features.engineering")`.
  </action>
  <verify>
    <automated>cd /Users/tirunelvelynagarajan/Desktop/EarthquakeAstrology/Earthquake\ 2026 && pytest tests/test_engineering.py::TestGridCells -x -q</automated>
  </verify>
  <done>TestGridCells all pass; compute_grid_coords handles negative latitudes and boundary values correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement country extraction, EQ index, and country map</name>
  <files>pipeline/features/engineering.py, tests/test_engineering.py</files>
  <behavior>
    - extract_country("Southern Sumatra, Indonesia") == "Indonesia"
    - extract_country("Bismarck Sea") == "Bismarck Sea"
    - extract_country("Kodiak Island region, Alaska") == "Alaska"
    - extract_country(None) == "Unknown"
    - extract_country("") == "Unknown"
    - extract_country(float('nan')) == "Unknown"
    - build_eq_index(usgs_df) returns pd.Series with MultiIndex levels: (date as datetime.date, grid_lat as int, grid_lon as int) — all values are int 1
    - Two USGS rows on same date in same cell -> build_eq_index produces ONE entry for that (date, cell) combo
    - build_country_map(usgs_df) returns dict mapping (grid_lat: int, grid_lon: int) -> str country label (most common country for that cell)
  </behavior>
  <action>
    Implement `extract_country(place) -> str`:
    ```python
    def extract_country(place) -> str:
        if place is None or (isinstance(place, float) and np.isnan(place)) or str(place).strip() == "":
            return "Unknown"
        parts = str(place).split(",")
        return parts[-1].strip()
    ```

    Implement `build_eq_index(usgs_df: pd.DataFrame) -> pd.Series`:
    - Parse date: `usgs_df['date'] = pd.to_datetime(usgs_df['time']).dt.normalize().dt.date`
    - Compute grid cols: vectorized np.floor
    - Group by (date, grid_lat, grid_lon) — this collapses multiple events in same cell+date
    - Return `pd.Series(1, index=pd.MultiIndex.from_tuples(...), name='EQIndicator')` — one entry per unique (date, cell) triple
    - Use `.drop_duplicates(subset=['date', 'grid_lat', 'grid_lon'])` before building the index to ensure no duplicates

    Implement `build_country_map(usgs_df: pd.DataFrame) -> dict[tuple[int, int], str]`:
    - Compute grid_lat/grid_lon and country columns
    - For each (grid_lat, grid_lon) cell, find the most frequent country value
    - Return as dict: `{(grid_lat, grid_lon): most_common_country, ...}`
    - This is used in Plan 04 to add the `country` column to each broadcast row

    Update TestCountryParsing, TestEQIndicator, TestEQIndicatorCollapse to pass (remove xfail, add real assertions).
  </action>
  <verify>
    <automated>cd /Users/tirunelvelynagarajan/Desktop/EarthquakeAstrology/Earthquake\ 2026 && pytest tests/test_engineering.py::TestCountryParsing tests/test_engineering.py::TestEQIndicator tests/test_engineering.py::TestEQIndicatorCollapse -x -q</automated>
  </verify>
  <done>All three test classes pass; extract_country handles None/NaN without exception; EQ index has no duplicate (date, cell) entries</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_engineering.py::TestGridCells tests/test_engineering.py::TestCountryParsing tests/test_engineering.py::TestEQIndicator tests/test_engineering.py::TestEQIndicatorCollapse -x -q` — all green
2. `pytest tests/test_engineering.py -x -q` — no new failures introduced (other tests remain xfail/skip)
3. Quick smoke test: `python -c "from pipeline.features.engineering import build_active_cells, build_eq_index; import pandas as pd; df = pd.read_csv('data/raw/usgs_earthquakes.csv'); cells = build_active_cells(df); print(f'Active cells: {len(cells)}')"` — prints ~901
</verification>

<success_criteria>
- compute_grid_coords correctly implements floor(x/5)*5 for both positive and negative coordinates
- build_active_cells dynamically computes from USGS data (not hardcoded count)
- extract_country handles all USGS place field edge cases (no comma, None, NaN, empty string)
- build_eq_index has no duplicate (date, cell) entries; all values are 1
- build_country_map assigns a country label to every active cell
- FEAT-04 and FEAT-05 test cases pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-engineering/02-02-SUMMARY.md`
</output>
