---
phase: 02-feature-engineering
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pipeline/features/__init__.py
  - pipeline/features/engineering.py
  - tests/test_engineering.py
  - pyproject.toml
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04, FEAT-05]

must_haves:
  truths:
    - "pytest tests/test_engineering.py -x -q runs without ImportError"
    - "All 10 test stubs exist as skipped/xfail so subsequent waves can make them pass"
    - "pyarrow is installed and importable in the project venv"
    - "pipeline/features/engineering.py imports cleanly with no AttributeError"
  artifacts:
    - path: "pipeline/features/__init__.py"
      provides: "Python package declaration for features module"
    - path: "pipeline/features/engineering.py"
      provides: "Module stub with function signatures matching test expectations"
    - path: "tests/test_engineering.py"
      provides: "All 10 test class stubs for FEAT-01 through FEAT-05"
  key_links:
    - from: "tests/test_engineering.py"
      to: "pipeline/features/engineering.py"
      via: "import pipeline.features.engineering"
      pattern: "from pipeline.features"
---

<objective>
Install pyarrow, create the pipeline/features Python package, write all Phase 2 test stubs, and scaffold the engineering module with correct function signatures.

Purpose: Wave 0 gate — every downstream task depends on this. Tests must exist before Wave 1 writes production code, satisfying the Nyquist rule. Stubs allow pytest to collect without error.
Output: `pipeline/features/__init__.py`, `pipeline/features/engineering.py` (stub), `tests/test_engineering.py` (10 test class stubs), pyarrow added to pyproject.toml.
</objective>

<execution_context>
@/Users/tirunelvelynagarajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tirunelvelynagarajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-feature-engineering/02-RESEARCH.md
@.planning/phases/02-feature-engineering/02-VALIDATION.md

@pipeline/data/ephemeris.py
@pipeline/data/usgs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pyarrow and create pipeline/features package</name>
  <files>pipeline/features/__init__.py, pyproject.toml</files>
  <action>
    Run `uv add pyarrow` from the project root to add pyarrow to pyproject.toml dependencies. pyarrow is required for `pd.to_parquet()` on the 8.5M-row test set — CSV is not viable at that scale.

    Create `pipeline/features/__init__.py` as an empty file (just a docstring is fine: `"""Feature engineering pipeline for Phase 2."""`).

    Create `data/processed/` directory if it does not exist (this is where parquet output will land).

    Verify pyarrow imports cleanly: `python -c "import pyarrow; print(pyarrow.__version__)"`.
  </action>
  <verify>
    <automated>cd /Users/tirunelvelynagarajan/Desktop/EarthquakeAstrology/Earthquake\ 2026 && python -c "import pyarrow; print(pyarrow.__version__)" && python -c "from pipeline.features import __init__" && echo "OK"</automated>
  </verify>
  <done>pyarrow importable, pipeline/features/__init__.py exists, data/processed/ directory exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test stubs and engineering.py module skeleton</name>
  <files>tests/test_engineering.py, pipeline/features/engineering.py</files>
  <behavior>
    - TestGridCells: build_active_cells(usgs_df) returns a set of (int, int) tuples; compute_grid_coords(lat, lon) returns (floor(lat/5)*5, floor(lon/5)*5) as ints
    - TestCountryParsing: extract_country("Southern Sumatra, Indonesia") == "Indonesia"; extract_country("Bismarck Sea") == "Bismarck Sea"; extract_country(None) == "Unknown"; extract_country("") == "Unknown"
    - TestEQIndicator: build_eq_index(usgs_df) returns pd.Series with MultiIndex (date, grid_lat, grid_lon) and value 1 for each unique (date, cell) combo; a known event date+cell maps to 1
    - TestEQIndicatorCollapse: two USGS events on same date in same cell produce a single EQIndicator=1 (not 2)
    - TestColumnInventory: encode_ephemeris(ephe_df) output contains columns matching `{p}_lon_sin`, `{p}_lon_cos`, `{p}_sign_num_sin`, `{p}_sign_num_cos`, `{p}_retro`, `{p}_nakshatra_num_sin`, `{p}_nakshatra_num_cos` for each planet; aspect columns are present
    - TestNoRawColumns: encode_ephemeris(ephe_df) output contains NO columns matching `{p}_lon` (raw), `{p}_sign_num` (raw), `{p}_sign` (text), `{p}_nakshatra` (text)
    - TestCyclicalEncoding: for a known sun_lon=0.0, sun_lon_sin==0.0 and sun_lon_cos==1.0; for sun_lon=90.0, sin≈1.0 and cos≈0.0; tithi_sin/tithi_cos columns present
    - TestTemporalSplit: assert_no_temporal_leakage(train_dates, test_dates) raises AssertionError when given leaking dates; passes when split is clean
    - TestEncoderFitScope: fit_nakshatra_encoder(pre2000_df) returns a fitted OneHotEncoder; calling transform on a post-2000 df with unknown nakshatra produces zero vector (not error)
    - TestDownsamplingScope: downsample_negatives(df, ratio=10, random_state=42) applied to a pre-2000 chunk returns len(positives)*11 rows with all positives preserved
  </behavior>
  <action>
    Create `tests/test_engineering.py` with 10 test classes (names exactly as listed in VALIDATION.md). Each test class has at minimum one test method that is marked `pytest.mark.xfail(reason="not implemented")` so the suite collects cleanly. Write the full expected assertions as comments so Wave 1 and Wave 2 tasks know exactly what to implement.

    Create `pipeline/features/engineering.py` with stub function signatures matching what the tests import:
    - `compute_grid_coords(lat: float, lon: float) -> tuple[int, int]` — raises NotImplementedError
    - `build_active_cells(usgs_df: pd.DataFrame) -> set[tuple[int, int]]` — raises NotImplementedError
    - `extract_country(place: str | None) -> str` — raises NotImplementedError
    - `build_eq_index(usgs_df: pd.DataFrame) -> pd.Series` — raises NotImplementedError
    - `encode_ephemeris(ephe_df: pd.DataFrame) -> pd.DataFrame` — raises NotImplementedError
    - `encode_cyclic(series: pd.Series, period: float) -> tuple[pd.Series, pd.Series]` — raises NotImplementedError
    - `compute_tithi(sun_lon: float, moon_lon: float) -> tuple[int, str]` — raises NotImplementedError
    - `fit_nakshatra_encoder(pre2000_df: pd.DataFrame) -> object` — raises NotImplementedError
    - `assert_no_temporal_leakage(train_dates, test_dates) -> None` — raises NotImplementedError
    - `downsample_negatives(df: pd.DataFrame, ratio: int, random_state: int) -> pd.DataFrame` — raises NotImplementedError

    Import pattern in test file: `from pipeline.features.engineering import compute_grid_coords, build_active_cells, ...`

    PLANETS constant must be accessible: import from ephemeris.py or redefine in engineering.py. Check pipeline/data/ephemeris.py for the PLANETS dict/list and replicate in engineering.py as `PLANETS = [...]`.
  </action>
  <verify>
    <automated>cd /Users/tirunelvelynagarajan/Desktop/EarthquakeAstrology/Earthquake\ 2026 && pytest tests/test_engineering.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects all 10 test classes without ImportError; tests are xfail or skipped (not erroring on collection); exit code is 0</done>
</task>

</tasks>

<verification>
1. `python -c "import pyarrow"` exits 0
2. `pytest tests/test_engineering.py -x -q` exits 0 (all xfail or skipped)
3. `python -c "from pipeline.features.engineering import compute_grid_coords, build_active_cells, extract_country, build_eq_index, encode_ephemeris, assert_no_temporal_leakage"` exits 0
4. `data/processed/` directory exists
</verification>

<success_criteria>
- pyarrow installed and importable
- pipeline/features/ is a valid Python package
- All 10 test class stubs exist and pytest collects without error
- engineering.py exposes all function signatures downstream tasks will implement
- data/processed/ output directory exists
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-engineering/02-01-SUMMARY.md`
</output>
