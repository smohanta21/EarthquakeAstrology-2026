---
phase: 02-feature-engineering
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - pipeline/features/engineering.py
  - tests/test_engineering.py
autonomous: true
requirements: [FEAT-01, FEAT-02]

must_haves:
  truths:
    - "encode_ephemeris output contains {p}_lon_sin and {p}_lon_cos for all 13 planets"
    - "encode_ephemeris output contains {p}_sign_num_sin and {p}_sign_num_cos for all 13 planets"
    - "encode_ephemeris output contains {p}_nakshatra_num_sin and {p}_nakshatra_num_cos for all 13 planets"
    - "encode_ephemeris output contains tithi_sin and tithi_cos columns derived from sun_lon and moon_lon"
    - "No raw *_lon, *_sign_num, *_sign (text), *_nakshatra (text), *_nakshatra_num columns remain in output"
    - "All 390 aspect boolean columns are preserved as int (0/1)"
    - "All 13 retrograde flag columns are preserved as int (0/1)"
    - "fit_nakshatra_encoder accepts only pre-2000 rows and returns a fitted scikit-learn OneHotEncoder"
    - "The fitted encoder produces 351 output columns (13 planets x 27 nakshatras)"
  artifacts:
    - path: "pipeline/features/engineering.py"
      provides: "encode_ephemeris, encode_cyclic, compute_tithi, fit_nakshatra_encoder — all fully implemented"
      exports: ["encode_ephemeris", "encode_cyclic", "compute_tithi", "fit_nakshatra_encoder"]
  key_links:
    - from: "pipeline/features/engineering.py:encode_ephemeris"
      to: "data/raw/ephemeris.csv"
      via: "transforms 469-column raw ephemeris to ~832-column encoded dataframe"
      pattern: "encode_cyclic.*period.*drop.*sin.*cos"
    - from: "pipeline/features/engineering.py:fit_nakshatra_encoder"
      to: "sklearn.preprocessing.OneHotEncoder"
      via: "fit on pre-2000 nakshatra name columns only"
      pattern: "OneHotEncoder.*handle_unknown.*ignore.*sparse_output.*False"
---

<objective>
Implement all ephemeris encoding functions: cyclical sin/cos encoding, tithi computation, nakshatra one-hot fitting, and the master encode_ephemeris function that transforms the 469-column raw ephemeris into the ~832-column encoded form.

Purpose: These functions are the other half of the Wave 2 matrix builder. Plan 04 calls encode_ephemeris once on the full ephemeris DataFrame before chunking, so the encoded frame is ready to broadcast to cells.
Output: Five functions implemented in pipeline/features/engineering.py; TestColumnInventory, TestNoRawColumns, TestCyclicalEncoding, TestEncoderFitScope tests all pass.
</objective>

<execution_context>
@/Users/tirunelvelynagarajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tirunelvelynagarajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-feature-engineering/02-01-SUMMARY.md
@.planning/phases/02-feature-engineering/02-RESEARCH.md
@pipeline/features/engineering.py
@tests/test_engineering.py
@pipeline/data/ephemeris.py

<interfaces>
<!-- From pipeline/data/ephemeris.py — confirmed PLANETS list and NAKSHATRAS -->
<!-- Column naming convention: {planet}_lon, {planet}_sign_num, {planet}_sign,
     {planet}_retro, {planet}_nakshatra_num, {planet}_nakshatra -->
<!-- Aspects: {p1}_{p2}_{aspect_type} where aspect types are:
     conjunction, opposition, trine, square, sextile (5 types) -->
<!-- 13 planets: sun, moon, mercury, venus, mars, jupiter, saturn,
     uranus, neptune, pluto, north_node, south_node, chiron -->

ephemeris.csv column groups (469 total):
  1 date column
  13 {p}_lon (float, 0-360)
  13 {p}_sign_num (int, 1-12)
  13 {p}_sign (str, e.g. "Aries")
  13 {p}_retro (bool -> int 0/1)
  13 {p}_nakshatra_num (int, 1-27)
  13 {p}_nakshatra (str, e.g. "Ashwini")
  390 {p1}_{p2}_{aspect} (bool -> int 0/1)

Tithi formula (from RESEARCH.md Archive audit):
  tithi_idx = int((moon_lon - sun_lon) % 360 / 12)  # 0-29
  tithi_sin = sin(tithi_idx * 2*pi / 30)
  tithi_cos = cos(tithi_idx * 2*pi / 30)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement encode_cyclic, compute_tithi, and ephemeris column transforms</name>
  <files>pipeline/features/engineering.py, tests/test_engineering.py</files>
  <behavior>
    - encode_cyclic(pd.Series([0.0]), period=360) -> (sin_series with value 0.0, cos_series with value 1.0)
    - encode_cyclic(pd.Series([90.0]), period=360) -> (sin≈1.0, cos≈0.0)
    - encode_cyclic(pd.Series([1.0]), period=12) -> (sin(2π/12), cos(2π/12)) = (sin(30°), cos(30°)) ≈ (0.5, 0.866)
    - compute_tithi(sun_lon=0.0, moon_lon=0.0) -> (0, "SP1") [diff=0, idx=0]
    - compute_tithi(sun_lon=0.0, moon_lon=180.0) -> (15, "KP1") [diff=180, idx=15]
    - compute_tithi(sun_lon=350.0, moon_lon=5.0) -> (1, "SP2") [diff=15, idx=1]
    - After encode_ephemeris, columns sun_lon_sin, sun_lon_cos, tithi_sin, tithi_cos exist
    - After encode_ephemeris, columns sun_lon, sun_sign_num, sun_sign, sun_nakshatra, sun_nakshatra_num do NOT exist
  </behavior>
  <action>
    Implement `encode_cyclic(series: pd.Series, period: float) -> tuple[pd.Series, pd.Series]`:
    ```python
    def encode_cyclic(series: pd.Series, period: float) -> tuple[pd.Series, pd.Series]:
        radians = series * (2 * np.pi / period)
        return np.sin(radians), np.cos(radians)
    ```

    Implement `compute_tithi(sun_lon: float, moon_lon: float) -> tuple[int, str]` using the lookup table from RESEARCH.md:
    - TITHIS list of 30 names (SP1-SP15, then FM for idx 14, KP1-KP14, NM for idx 29)
    - Wait — the RESEARCH.md listing shows FM at position 14 (0-indexed), NM at position 29. Use exactly: SP1..SP14, FM, KP1..KP14, NM
    - `diff = (moon_lon - sun_lon) % 360; tithi_idx = int(diff / 12); return tithi_idx, TITHIS[tithi_idx]`

    Define module-level `PLANETS` list matching ephemeris.py conventions (copy from ephemeris.py). This must be importable as `from pipeline.features.engineering import PLANETS`.

    Implement `encode_ephemeris(ephe_df: pd.DataFrame) -> pd.DataFrame` as a thin orchestration stub that:
    1. Computes and adds tithi_sin/tithi_cos from sun_lon and moon_lon
    2. For each planet, adds {p}_lon_sin, {p}_lon_cos (period=360)
    3. For each planet, adds {p}_sign_num_sin, {p}_sign_num_cos (period=12)
    4. For each planet, adds {p}_nakshatra_num_sin, {p}_nakshatra_num_cos (period=27)
    5. Converts all retro bool columns to int (0/1): `df[f'{p}_retro'] = df[f'{p}_retro'].astype(int)`
    6. Converts all aspect bool columns to int (0/1): identify by column naming pattern `{p1}_{p2}_{aspect}`
    7. Drops raw columns: all `{p}_lon`, `{p}_sign_num`, `{p}_sign`, `{p}_nakshatra`, `{p}_nakshatra_num` columns
    8. Does NOT yet apply nakshatra one-hot (that requires pre-2000 fit first — handled in Plan 04)
    9. Returns the transformed DataFrame with `date` preserved as index or column

    NOTE on column count at this stage: after encode_ephemeris (before nakshatra one-hot), the frame has:
      26 lon sin/cos + 26 sign_num sin/cos + 26 nakshatra_num sin/cos + 13 retro + 390 aspect + 2 tithi = 483 columns + date
    Nakshatra name one-hot (351 cols) is added in Plan 04 via apply_nakshatra_encoding() using the fitted encoder.

    Update TestCyclicalEncoding to pass (remove xfail, add real assertions using np.isclose for float comparisons).
    Update TestColumnInventory and TestNoRawColumns partially — they will fully pass after Task 2 adds nakshatra one-hot columns.
  </action>
  <verify>
    <automated>cd /Users/tirunelvelynagarajan/Desktop/EarthquakeAstrology/Earthquake\ 2026 && pytest tests/test_engineering.py::TestCyclicalEncoding -x -q</automated>
  </verify>
  <done>TestCyclicalEncoding passes; encode_cyclic handles period=360, period=12, period=27; compute_tithi returns correct (int, str) tuple; encode_ephemeris adds sin/cos columns and drops raw columns</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fit_nakshatra_encoder and apply_nakshatra_encoding</name>
  <files>pipeline/features/engineering.py, tests/test_engineering.py</files>
  <behavior>
    - fit_nakshatra_encoder(pre2000_df) returns a fitted sklearn OneHotEncoder
    - The encoder has 13 feature inputs (one per planet nakshatra name column)
    - encoder.get_feature_names_out() returns 351 column names (13 * 27, assuming all 27 nakshatras appear in pre-2000 data)
    - apply_nakshatra_encoding(df, encoder) transforms the 13 nakshatra name columns into 351 one-hot columns and drops the original string columns
    - Calling apply_nakshatra_encoding with an unseen nakshatra name (not in pre-2000 vocab) produces all-zero columns for that planet (handle_unknown='ignore' behavior)
    - After both encode_ephemeris + apply_nakshatra_encoding, no object-dtype columns remain (except 'date' if kept as string)
  </behavior>
  <action>
    Add `NAKSHATRA_COLS = [f'{p}_nakshatra' for p in PLANETS]` as a module-level constant.

    Implement `fit_nakshatra_encoder(pre2000_df: pd.DataFrame) -> OneHotEncoder`:
    ```python
    from sklearn.preprocessing import OneHotEncoder
    import numpy as np

    def fit_nakshatra_encoder(pre2000_df: pd.DataFrame):
        encoder = OneHotEncoder(
            handle_unknown='ignore',
            sparse_output=False,
            dtype=np.uint8,
        )
        encoder.fit(pre2000_df[NAKSHATRA_COLS])
        return encoder
    ```
    The input pre2000_df must still have the raw `{p}_nakshatra` string columns (i.e., call this BEFORE encode_ephemeris drops those columns, or pass ephe_df filtered to pre-2000 dates directly).

    Implement `apply_nakshatra_encoding(df: pd.DataFrame, encoder: OneHotEncoder) -> pd.DataFrame`:
    - Calls `encoder.transform(df[NAKSHATRA_COLS])` to get the uint8 array
    - Gets column names via `encoder.get_feature_names_out(NAKSHATRA_COLS)` — produces names like `sun_nakshatra_Ashwini`, `moon_nakshatra_Bharani`, etc.
    - Creates a DataFrame from the array, assigns column names
    - Drops the original NAKSHATRA_COLS string columns from df
    - Concatenates the new one-hot columns
    - Returns the result

    Also add `save_encoder(encoder, path: str)` using `joblib.dump(encoder, path)` and `load_encoder(path: str)` using `joblib.load(path)`. These will be called in Plan 04 to persist the fitted encoder to `data/processed/nakshatra_encoder.pkl`.

    Update TestEncoderFitScope to pass: create a mini DataFrame with 13 nakshatra name columns (27 nakshatras each, repeated) as fixture; assert encoder.categories_ has 13 entries each of length 27.

    Update TestColumnInventory and TestNoRawColumns to pass: after calling encode_ephemeris then apply_nakshatra_encoding, assert nakshatra name string columns gone and 351 new one-hot cols present.
  </action>
  <verify>
    <automated>cd /Users/tirunelvelynagarajan/Desktop/EarthquakeAstrology/Earthquake\ 2026 && pytest tests/test_engineering.py::TestEncoderFitScope tests/test_engineering.py::TestColumnInventory tests/test_engineering.py::TestNoRawColumns -x -q</automated>
  </verify>
  <done>All three test classes pass; OneHotEncoder with handle_unknown='ignore' returns zero vector for unseen nakshatra; no object-dtype columns remain after full encoding pipeline</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_engineering.py::TestCyclicalEncoding tests/test_engineering.py::TestColumnInventory tests/test_engineering.py::TestNoRawColumns tests/test_engineering.py::TestEncoderFitScope -x -q` — all green
2. `pytest tests/test_engineering.py -x -q` — no regressions in Plan 02 tests
3. Quick smoke: `python -c "from pipeline.features.engineering import encode_cyclic, compute_tithi, PLANETS; import numpy as np, pandas as pd; s, c = encode_cyclic(pd.Series([0.0, 90.0, 180.0, 270.0]), 360); print(list(zip(s, c)))"` — prints [(0,1), (~1,~0), (~0,-1), (~-1,~0)]
</verification>

<success_criteria>
- encode_cyclic produces correct sin/cos for degrees (period=360) and signs (period=12)
- compute_tithi correctly handles the moon-sun difference modulo 360
- encode_ephemeris drops all raw lon/sign_num/sign/nakshatra_num/nakshatra_name columns and adds sin/cos replacements plus tithi_sin/cos
- fit_nakshatra_encoder fit on pre-2000 data only; apply_nakshatra_encoding adds 351 one-hot columns
- FEAT-01 (column inventory) and FEAT-02 (cyclical encoding) tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-engineering/02-03-SUMMARY.md`
</output>
